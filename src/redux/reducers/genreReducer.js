const initialState = {
    isLoading: false,
    error: null,
    genres: [],
  }
const genreReducer = (state = initialState , { type, payload })=>{
  console.log(type)
  switch(type){
    
  }
return state;
};


export default genreReducer;